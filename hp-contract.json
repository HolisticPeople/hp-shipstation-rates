{
  "plugin": "hp-shipstation-rates",
  "repo": "HolisticPeople/hp-shipstation-rates",
  "schema_version": "1.0",
  "owns": [
    "Storefront live shipping rate quoting for classic WooCommerce checkout via the hp_shipstation WC shipping method (ShipStation-backed USPS/UPS/FedEx rates, carrier-qualified service allow-list with custom display names, carrier badges, quote caching)",
    "Pure upfront delivery estimates from explicitly approved service/destination transit policies, including confirmed handling cutoff and calendar; default unavailable"
  ],
  "must_not_own": [
    "cross-plugin source mutation without registered contract",
    "ShipStation order export, tracking sync, or shipnotify handling (official woocommerce-shipstation-integration plugin plus HP-Core/HP-Inventory/HP-Checkout seams)",
    "ShipStation credential storage (canonical store is HP-Core ShipStationSettings)"
  ],
  "consumes": [
    "HP-Core shared ShipStation credentials via hp_ss_get_shipstation_credentials() -> HP_Core\\Services\\ShipStationSettings (option hp_core_shipstation_settings; fail-soft direct option fallback)",
    "HP-Core central rating service via \\HP_Core\\get_service('shipstation_rates') / HP_Core\\Services\\ShipStationRatesService::getCarrierRates() (since 4.1.0; fail-soft inline ShipStation V1 HTTP fallback when the provider is absent or older)"
  ],
  "exposes": {
    "cpts": [],
    "rest": [],
    "shortcodes": [],
    "providers": [
      "hp_ss_get_enabled_service_codes() - legacy enabled ShipStation service codes for consumers",
      "hp_ss_get_enabled_service_keys() - carrier-qualified enabled ShipStation service identities",
      "hp_ss_get_enabled_carrier_codes() - enabled V1 carrier codes shared by HP Core, EAO, checkout, and funnel consumers",
      "hp_ss_get_delivery_promise_v1(array $context): array"
    ]
  },
  "verification": {
    "staging": {
      "status": "unverified"
    },
    "production": {
      "status": "unverified"
    }
  }
}
